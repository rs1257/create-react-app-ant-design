import forecastMargins from '../../../data/forecastMargins.json';

// eslint-disable-next-line no-console
console.log(forecastMargins);

// interface DataType {

// }

// const {
//   mntriggerdemandforecastweekly: { header, mnData, demandData },
// } = forecastMargins;

// const headers = header.map((cell, index) => {
//   return {
//     title: cell,
//     dataIndex: index + 1,
//   };
// });

// const getFormattedData = () => {
//   const rowOne = mnData.reduce((result, item, index) => {
//     result[index + 1] = item;
//     return result;
//   }, {});
// };
